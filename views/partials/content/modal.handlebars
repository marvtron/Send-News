<div class="modalfade" id="{{this.id}}" tabindex="-1" role="dialog" aria-labelledby="notesModal">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <button type="submit" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                <h4 class="modal-title" id="myModalLabel">Notes for {{this.title}}</h4>
            </div>
            <div class="modal-body">
                {{#if this.notes}}
                    {{#each this.notes}}
                        <div class="well">
                            <div class="row">
                                <div class="col col-md-10">
                                    <p>{{this.text}}</p>
                                </div>
                                <div class="col col-md-2">
                                    <form action="/api/notes/{{this.id}}?_method=DELETE" method="POST" class="form-inline">
                                        <button type="submit" class="btn btn-danger">X</button>
                                    </form>
                                </div>
                            </div>
                        </div>
                    {{/each}}
                    {{else}}
                        <div class="well">
                            <p>This story does not have any notes attached</p>
                        </div>
                    {{/if}}
            </div>
            <div class="modal-footer">
                <div class="row">
                    <div class="col col-md-8">
                        <form action="/api/notes/{{this.id}}" class="form-inline" method="POST">
                            <input type="text" class="form-control" id="text" name="text" placeholder="Add a new note...">
                            <button type="submit" class="btn btn-info">Add Note</button>
                        </form>
                    </div>
                    <div class="col col-md-4">
                        <button type="submit" class="btn btn-default" data-dismiss="modal">Close</button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>